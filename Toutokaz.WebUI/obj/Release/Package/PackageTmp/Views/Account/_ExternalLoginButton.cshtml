@model ICollection<AuthenticationClientData>                   
 @if (Model.Count == 0)
                    {
    
                    }
                    else
                    {
                        using (Html.BeginForm("ExternalLogin", "Account",FormMethod.Post, new { ReturnUrl = ViewBag.ReturnUrl, id="fb_form", name="fb_form"}))
                        {
                             @Html.AntiForgeryToken()

                            foreach (AuthenticationClientData p in Model)
                            {
                                //object src = @p.ExtraData["Icon"];
                                //   <input type="image" src="@Url.Content(src.ToString())" alt="Icon for @p.DisplayName" name="provider" value="@p.AuthenticationClient.ProviderName" title="Se connecter avec mon compte @p.DisplayName"/>
                                //  <img src="@Url.Content(src.ToString())" alt="Icon for @p.DisplayName" />
                                // <button class="btn btn-primary round"  type="submit" name="provider" value="@p.AuthenticationClient.ProviderName" title="Se connecter avec mon compte @p.DisplayName">Se connecter avec @p.DisplayName</button>

                                <button class="btn btn-block btn-lg btn-social btn-facebook round" type="submit" name="provider" value="@p.AuthenticationClient.ProviderName" >
                                    <i class="fa fa-facebook"></i> Se connecter avec @p.DisplayName
                                </button>
                            }

                        }
                    }